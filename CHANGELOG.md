# Changelog

## 0.1.0 — 2026-09-02

Initial unified release with ten checks and an aggregate mode.

This clean-history repository consolidates the behavior prototyped in `action-lock`, `env-contract`, `caseguard`, `shape-lock`, `test-shuffle`, `license-lens`, `webhook-scrub`, `boundary-check`, `readme-paths`, and `git-weight`. Those precursor repositories were superseded after this release passed CI.
